#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(){

	int n,p,k;
	
	scanf("%d %d %d",&n,&p,&k);
	
	int *int_list = malloc(sizeof(int)*n);
	
	int i;
	for(i=0;i<n;i++){
		scanf("%d",&int_list[i]);
	}
	
	int j;
	
	for(i=0;i<n-1;i++){
		for(j=i+1;j<n;j++){
			if()
		}
	}
	
	return 0;
}